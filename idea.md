# 소개팅 장소 자동 탐색 에이전트

## 한 줄 소개

사용자 취향에 맞는 소개팅 장소를 네이버지도에서 자동으로 탐색하고, 후보 장소를 점수화해 리포트로 정리해주는 AI 에이전트.

## 문제

소개팅 장소를 고를 때는 생각보다 많은 시간이 듦.

- 네이버지도에서 지역과 카테고리를 직접 검색해야 함.
- 장소별 사진, 리뷰, 분위기, 좌석 간격 등을 일일이 확인해야 함.
- 소개팅에 적합한지 판단하려면 여러 장소를 비교해야 함.
- 최종 후보를 정리하고 공유하는 과정도 번거로움.

## 해결 아이디어

사용자가 원하는 조건을 입력하면, 에이전트가 네이버지도를 직접 탐색하면서 장소를 분석함.

에이전트는 장소 목록을 순차적으로 확인하고, 사진과 리뷰를 기반으로 소개팅 적합도를 점수화함. 탐색 결과는 실시간으로 누적되며, 최종적으로 점수 높은 순서의 리포트로 저장됨.

## 사용 흐름

1. 사용자가 웹에서 취향과 조건을 설정함.
2. 사용자가 에이전트를 실행함.
3. 에이전트가 네이버지도에서 특정 지역의 장소를 탐색함.
4. 진행 화면에서 에이전트의 판단 과정과 브라우저 탐색 화면을 실시간으로 확인함.
5. 에이전트가 장소를 분석할 때마다 리포트가 갱신됨.
6. 사용자는 탐색 중에도 현재까지의 리포트를 확인할 수 있음.
7. 탐색이 끝나면 최종 리포트가 파일과 인박스에 저장됨.

## 사용자 취향 설정

사용자 취향은 크게 두 가지로 나뉨.

### 사전 필터링 조건

장소를 자세히 분석하기 전에 제외할지 판단하는 기준.

예시:

- 카테고리: 양식, 일식
- 리뷰 수: 50개 이상

### 점수 조건

장소를 분석하고 점수화할 때 사용하는 기준.

예시:

- 내부 사진: 어둡고 차분한 분위기
- 좌석: 테이블 간격이 넓고 대화하기 좋은 구조
- 리뷰: 깔끔함, 조용함, 대화하기 좋음 등의 표현

## 핵심 기능

### 자동 장소 탐색

네이버지도에서 특정 역 또는 지역을 기준으로 후보 장소를 찾음. 검색 결과 목록을 순차적으로 확인하면서 각 장소를 분석함.

### 장소 필터링

카테고리, 리뷰 수, 위치 등 사용자가 설정한 기본 조건에 맞지 않는 장소는 깊게 분석하지 않고 제외함. 제외된 장소도 리포트에 기록함.

### 사진 기반 분위기 분석

장소의 내부 사진을 가져와 소개팅에 적합한 분위기인지 판단함. 조명, 좌석 배치, 공간감 등 사진에서 확인 가능한 요소를 기준으로 점수화함.

### 리뷰 기반 평가

최신 리뷰를 기반으로 장소의 실제 분위기와 이용자 반응을 분석함. 소개팅에 긍정적인 표현과 부정적인 표현을 종합해 점수화함.

### 리포트 생성

분석한 장소를 점수 높은 순으로 정리함. 각 장소별로 사진 점수, 리뷰 점수, 최종 점수, 판단 근거를 함께 보여줌.

## 에이전트 구조

### 메인 에이전트

전체 탐색 흐름을 관리함. 네이버지도 검색 결과를 순회하고, 각 장소 분석을 서브 에이전트에게 요청함. 분석 결과를 받아 리포트에 반영함.

### 서브 에이전트

개별 장소 분석을 담당함. 필터링 조건을 먼저 확인하고, 통과한 장소에 대해 사진과 리뷰를 분석해 점수를 산출함.

## 리포트에 포함될 내용

- 장소명
- 위치
- 카테고리
- 최종 점수
- 사진 기반 점수와 판단 근거
- 리뷰 기반 점수와 판단 근거
- 대표 내부 사진
- 주요 리뷰 요약
- 필터링으로 제외된 장소와 제외 사유

## 기술 방향

- client: React
- backend/agent: Python
- agent framework: LangChain, LangGraph
- browser automation: Playwright
- browser streaming: Playwright CDP 기반 스트리밍

## 기대 효과

- 소개팅 장소 탐색 시간을 줄일 수 있음.
- 사용자의 취향에 맞는 장소를 일관된 기준으로 비교할 수 있음.
- 단순 검색 결과가 아니라, 사진과 리뷰를 함께 분석한 리포트를 얻을 수 있음.
- 탐색 과정이 실시간으로 보이기 때문에 에이전트가 어떤 기준으로 판단하는지 확인 가능함.
